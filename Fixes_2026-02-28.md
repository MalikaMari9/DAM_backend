## 2026-02-28

- Added organization-side ACAG upload support under the existing uploads flow:
  - CSV validate/confirm/dupe endpoints for ACAG annual PM2.5 uploads
  - Manual ACAG record creation
  - Upload-history record listing, editing, and deletion for ACAG batches
- Added ACAG upload schemas for annual country-level PM2.5 records.
- Updated IMHE repository reads so `IMHE` rows win over `IMHEPred` rows when the exact row exists; predictions are now fallback only.
- Updated ACAG repository reads so `ACAG` rows win over `ACAGPred` rows for overlapping predicted years; predictions are fallback only.
- Updated the frontend organization dashboard:
  - Pollution organizations now have an `OpenAQ` / `ACAG` toggle
  - ACAG CSV uploads use the new ACAG endpoints
  - Added manual ACAG annual PM2.5 entry (`year`, `population_weighted_pm25`, `geographic_mean_pm25`)
  - Upload history can edit ACAG records
- Validation and duplicate behavior:
  - Main-collection duplicates are blocked
  - Predicted rows do not block actual uploads into the main collections
- Fixed ACAG + IHME map rendering so bivariate mode now falls back to ACAG-only or IHME-only country colors instead of leaving those countries gray.
- Added loading spinners for map data and GeoJSON layer fetches so the map no longer appears blank while layers are still loading.
- Added frontend console debug/error logs for ACAG/IHME map loads, pin loading, and GeoJSON fetch state, and hid the pollution-pin legend while in ACAG mode.
- Fixed ACAG fallback aggregation compatibility by removing `$regexReplace` from the Mongo pipeline; older Mongo versions now serve `/pollution/acag/*` without throwing backend 500s.
- Normalized effective pollution filter values before the analysis view renders, preventing transient OpenAQ requests with invalid ACAG metrics during source switches.
- Added a filter-bar loading state so the analysis controls show a spinner and stay disabled while filter options or visualization data are still updating.
- Replaced the tiny filter-bar loading badge with a blocking overlay across the analysis workspace, with a short hold so loading feedback remains visible instead of flickering.
- Optimized the heavy ACAG and IMHE summary/trend endpoints without adding new collections:
  - `country_summary` and `trend_by_year` now read main collections first
  - prediction collections are queried only as gap-fill for missing output groups
  - this preserves actual-over-predicted behavior while reducing read-time merge cost
- Simplified the Analysis frontend source selector:
  - removed `WHO` from the visible pollution-source options
  - made `ACAG` the default pollution source
  - made `Population-Weighted` the default ACAG pollution metric
- Reworked the Analysis table view:
  - health table now uses its own point-year summary query instead of reusing cached map data
  - ACAG now supports a `Combined` table mode that merges ACAG and IHME country summaries
  - pollution table no longer slices already paginated backend rows a second time
  - OpenAQ mixed table view now falls back to PM2.5 instead of doing broken multi-request pagination
- Kept the map on its local normalization logic to avoid changing working map behavior, and added a targeted `Guinea Bissau` -> `Guinea-Bissau` alias in that map normalizer.
- Updated the map GeoJSON loader to try a local `world.geo.json` first and fall back to the GitHub URL, with console logs showing which source succeeded or failed.
- Added table-local country normalization in `DataVisualization` so ACAG + IHME combined rows and table-side country filters align on canonical names like `Vietnam`, `Turkey`, `South Korea`, and `North Korea`, without touching the map logic.
- Added a narrow country-name cleanup for analysis display:
  - normalized `Brunei Darussalam` -> `Brunei` and `Eswatini` -> `Swaziland` for the map/table
  - hid `Western Europe`, `Cape Verde`, `Micronesia`, and `Sao Tome and Principe` from table-facing analysis UI
  - pushed partial ACAG+IHME combined rows to the end of the table
- Removed the private hardcoded country list from `CountrySidebar`:
  - `Analysis` now passes a shared `countryCatalog` from `src/data/countries.ts`
  - the sidebar options are centralized in one frontend source instead of being duplicated inside the component
  - the analysis catalog keeps the previous analysis-safe labels so existing health-country filtering behavior does not regress
- Updated the Analysis trend chart:
  - multi-country selections now aggregate into one trend (health summed, pollution averaged)
  - default chart range now starts at `2015` instead of `2020`
  - predicted years are marked with shaded forecast bands, lighter health bars, and a dashed pollution line segment
- Removed OpenAQ from the chart’s pollution trend:
  - the OpenAQ method dropdown is gone
  - OpenAQ now renders as a health-only trend with an explanatory note, instead of implying a reliable annual pollution line from sparse station data
- Updated the homepage hero to lead into AI prediction:
  - the hero search now routes to `/prediction` instead of `/analysis`
  - the placeholder and CTA are AI-oriented
  - the four example chips are short natural-language prediction prompts
  - the prediction page now reads `?prompt=` and auto-submits the initial question once on load
- Fixed the homepage announcements type mismatch in `Index.tsx`:
  - `AnnouncementsRenderer` now uses the existing typed `mapApiToAnnouncement` helper
  - this resolves the `setAnnouncements(mapped)` TypeScript error caused by a loosely inferred inline map result
- Added a homepage live-air hero card powered by IQAir:
  - new backend proxy endpoint `GET /air/live/nearest` reads `IQAIR_API_KEY` from `backend/.env`
  - the home page now has a `Use My Location` flow that calls browser geolocation, fetches the nearest live AQI snapshot, and shows the current status
  - the live-air card includes an `Ask AI About This` action that routes into `/prediction` with a prefilled prompt
- Adjusted the live-air card to match IQAir current-data behavior:
  - the homepage now shows `Main Concentration` from the current main pollutant details instead of assuming PM2.5 is always present
- Simplified the live-air card fields:
  - removed the concentration tile from the homepage IQAir widget
  - the live widget now focuses on AQI, main pollutant, and temperature
- Cleaned up Analysis filter contracts:
  - removed dead `healthArea` state/props and renamed the visible filter label to `Cause`
  - removed the unused `onYearRangeChange` prop from `DataVisualization` without changing current year-control behavior
- Fixed Analysis reset-year behavior for ACAG:
  - `Reset Filters` now resets the year to the effective ACAG latest year instead of the raw IMHE max year
- Fixed ACAG table pagination at the backend:
  - the ACAG list path now excludes hidden/non-country rows before pagination and before `total` is counted
  - this keeps the ACAG table row count and page count consistent for the frontend
- Updated UI copy to match the current product direction:
  - replaced the stale `Compare countries` prediction prompt with a risk-outlook prompt
  - updated the Prediction page intro text accordingly
  - refreshed footer data-source labels to match the ACAG/IHME/OpenAQ/IQAir stack
  - fixed the footer copyright encoding bug
- Added table export for the Analysis table tab:
  - added `CSV` and `JSON` export actions in the table header
  - `Health` and `Combined` export the full filtered in-memory dataset
  - `Pollution` now does a dedicated full fetch so export is not limited to the current visible page
- Integrated real DALY support into the AI service:
  - replaced the runtime `ihme_baseline.json` with Liam's DALY-capable baseline data
  - updated the health engine to support true DALY calculation alongside deaths
  - added `predict_attributable_dalys(...)` to the executive health wrapper and exported it
  - rewired the `HEALTH_DALYS` chat intent to return actual DALYs instead of falling back to deaths
  - updated DALY health-burden analytics to use the real DALY path instead of the old `deaths x 12.5` approximation
